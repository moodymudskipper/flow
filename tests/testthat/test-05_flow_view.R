# test_that("flow_view doesn't crash on a simple example", {
#   # expect_error(flow_view(rle), NA)
#   # expect_error(flow_view(rle, out = "html"), NA)
#   # tf <- tempfile(fileext = ".png")
#   # expect_error(flow_view(rle, out = tf), NA)
# })
